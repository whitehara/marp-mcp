/** Maximum file size allowed for processing (10MB) */
export const MAX_FILE_SIZE = 10 * 1024 * 1024;

// Slide validation constants (must match minorun365/marp-agent output_slide.py)
/** Maximum number of visual lines per slide */
export const MAX_LINES_PER_SLIDE = 9;
/** Maximum display width (in half-width characters) per line */
export const MAX_DISPLAY_WIDTH_PER_LINE = 48;
/** Maximum display width (in half-width characters) for a table row */
export const MAX_TABLE_ROW_WIDTH = 64;
